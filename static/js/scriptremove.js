const element = document.querySelector('.gcsc-more-maybe-branding-root');
if (element) {
  element.parentNode.removeChild(element);
}
const myElement = document.querySelector('.gcsc-more-maybe-branding-root');
if (myElement) {
  myElement.parentNode.removeChild(myElement);
}
